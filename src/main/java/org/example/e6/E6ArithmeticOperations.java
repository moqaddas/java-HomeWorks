package org.example.e6;

public class E6ArithmeticOperations {
    public static void main(String[] args) {
   float originalValue=8.2f;
   float result =originalValue*originalValue;
   //first way of printing the results.
   System.out.printf("The multiplication of %.1f with itself is equal to %.2f",originalValue, result);


        //second way of printing the results.
        // System.out.println("The multiplication of " +originalValue+ " with itself is equal to " +originalValue*originalValue+result);

       // first way of printing the results.
        // System.out.println("the multiplication of " +originalValue+" with itself is equal to "+result);

    }
}
